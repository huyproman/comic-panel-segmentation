# Release Notes — v0.1.1

Release Date: 2026-09-09  
Release Tag: `historical-reconstruction-v1.0.1`  
Validated Reference Environment: **Python 3.10**

---

## Highlights
- **Verified Built Distribution**: Built wheel and sdist verified in a completely clean Python 3.10 virtual environment with imports resolving strictly from site-packages.
- **Fixture Path Portability**: Added `COMICS_FIXTURE_MANIFEST` and `COMICS_FIXTURE_ROOT` environment support, ensuring 100% of the 141 tests pass cleanly across both source checkout and external wheel installation.
- **Canonical Historical Reproduction**: Establishes `comics_panel_extraction.historical_legacy` as the authoritative implementation of the published line segment analysis method, achieving **92.46% mIoU** on the 74 test pages (vs. published Table 2 at **92.34%**).
- **Three Clear Execution Modes**: Separates `historical-replay` (primary reproduction), `reconstructed` (operational end-to-end with M1 model), and `manuscript-faithful` (scientific research baseline).
- **100% Test Suite Green**: 141 tests passing covering regression, oracle parity, unit logic, and CLI commands.

---

## Known Provenance Caveats
- Exact Comic checkpoint and raw producer are closed as `UNRESOLVED` (non-blocking). M1 serves as the authoritative reconstructed Comic model.
- Exact post-processing producer revision is closed as `UNRESOLVED` (non-blocking; 407.85 mean XOR px/page residual).
